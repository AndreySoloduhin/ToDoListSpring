spring:
  application:
    name: ToDoListSpring
  datasource:
    url: jdbc:postgresql://localhost:5432/ToDoList
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update